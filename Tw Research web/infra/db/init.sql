-- Autopredator Database Initialization
-- This file runs when the Postgres container starts for the first time

-- Create development database (if not exists)
-- Note: POSTGRES_DB is already created by the image

-- You can add initial schema setup here
-- For example:

-- CREATE TABLE IF NOT EXISTS users (
--     id SERIAL PRIMARY KEY,
--     email VARCHAR(255) UNIQUE NOT NULL,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- Add any initial data or schema migrations as needed
-- For production, use proper migration tools like Alembic
