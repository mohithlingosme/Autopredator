# Legacy Autopredator Prototype

This folder contains the PHP-based prototype, static HTML pages, SQL dumps,
and other artifacts from the first version of Autopredator.

The PHP app in `legacy/php_app/` is now the primary stack (React/Node builds
were removed). Everything else here is kept for reference and static assets.
