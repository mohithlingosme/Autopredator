# Legacy Autopredator Prototype

This folder contains the old PHP-based prototype, static HTML pages, SQL dumps,
and other artifacts from the first version of Autopredator.

The current live stack is:

- `backend/` ? Django REST API
- `frontend/` ? Next.js app

Nothing in `legacy/` is used by the current Docker / deployment pipeline.
These files are kept only for reference and historical context.
